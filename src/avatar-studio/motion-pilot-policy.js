'use strict';
const fs=require('node:fs'); const path=require('node:path');
const { AvatarStudioError }=require('./domain');
const policyPath=path.resolve(__dirname,'../../config/avatar-studio/motion-pilot-policy.json');
function loadMotionPilotPolicy(){let policy;try{policy=JSON.parse(fs.readFileSync(policyPath,'utf8'));}catch(error){throw new AvatarStudioError(503,'MOTION_PILOT_POLICY_UNAVAILABLE','Motion Pilot policy cannot be loaded');}const r=policy?.route,refs=policy?.references;if(policy.schemaVersion!==1||!r?.provider||!r?.model||!r?.capability||r.plannedCalls!==1||r.automaticRetryAllowed!==false||!Number.isFinite(policy?.budget?.maximumAllowedCostUsd)||policy.budget.maximumAllowedCostUsd<=0||refs?.exactCount!==4||!Array.isArray(refs.providerOrder)||refs.providerOrder.length!==4||policy.review?.humanIdentityReviewRequired!==true)throw new AvatarStudioError(503,'MOTION_PILOT_POLICY_INVALID','Motion Pilot policy is invalid');return Object.freeze({...policy,route:Object.freeze({...r}),budget:Object.freeze({...policy.budget}),references:Object.freeze({...refs,providerOrder:Object.freeze(refs.providerOrder.map(Object.freeze))}),review:Object.freeze({...policy.review,failureReasonCodes:Object.freeze([...policy.review.failureReasonCodes])}),execution:Object.freeze({...policy.execution}),minorSafety:Object.freeze({...policy.minorSafety})});}
module.exports={loadMotionPilotPolicy,policyPath};
