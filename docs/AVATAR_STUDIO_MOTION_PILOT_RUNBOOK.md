# Avatar Studio Motion Pilot

1. Select the brand and avatar.
2. Inspect the four exact reference images and their evidence.
3. Run the zero-call preflight and inspect provider, model, call count, and cost.
4. Approve the immutable execution.
5. Run exactly one real generation.
6. Watch the persisted MP4 directly in Dashboard.
7. Record a human PASS or FAIL identity decision.

Technical PASS is not Identity PASS. Never retry automatically. If a provider output exists but downstream handling failed, use durable local recovery before considering any new paid call. Normal operation requires no SQL.
