import React from 'react';import {render,screen,fireEvent} from '@testing-library/react';import {describe,it,expect,vi} from 'vitest';import {VideoModelContractControls,modelContractDefaults} from '../src/VideoModelContractControls';
const contract={contractVersion:'seedance@1',providerSchemaVersion:'schema-1',pricing:{status:'UNKNOWN_CURRENT_PRICE'},inputModes:['TEXT_TO_VIDEO','FIRST_FRAME_IMAGE_TO_VIDEO','FIRST_LAST_FRAME','MULTIMODAL_REFERENCE'],capabilities:['TEXT_TO_VIDEO','MULTIMODAL_REFERENCE','NATIVE_AUDIO_GENERATION'],parameters:{duration:{values:{intelligent:-1,minimum:4,maximum:6},contentFactoryDefault:5},resolution:{values:['720p','1080p'],contentFactoryDefault:'720p'},aspectRatio:{values:['adaptive','16:9','9:16'],contentFactoryDefault:'16:9'},generateAudio:{contentFactoryDefault:false},watermark:{contentFactoryDefault:false},outputFormat:{values:['mp4'],contentFactoryDefault:'mp4'},seed:{type:'integer',optional:true}}};
describe('model-contract operator controls',()=>{it('renders values from backend contract metadata and emits explicit overrides',()=>{const defaults=modelContractDefaults(contract),change=vi.fn();render(<VideoModelContractControls contract={contract} value={defaults} onChange={change}/>);expect(screen.getByText(/seedance@1/)).toBeTruthy();expect(screen.getByRole('option',{name:'MULTIMODAL_REFERENCE'})).toBeTruthy();expect(screen.getByRole('option',{name:'INTELLIGENT (-1)'})).toBeTruthy();fireEvent.change(screen.getByLabelText('MODEL INPUT MODE'),{target:{value:'FIRST_LAST_FRAME'}});expect(change).toHaveBeenCalledWith({...defaults,resolvedInputMode:'FIRST_LAST_FRAME'});fireEvent.change(screen.getByLabelText('MODEL SEED'),{target:{value:'42'}});expect(change).toHaveBeenCalledWith({...defaults,seed:42});fireEvent.click(screen.getByLabelText('MODEL GENERATE AUDIO'));expect(change).toHaveBeenCalledWith({...defaults,generateAudio:true});});});
