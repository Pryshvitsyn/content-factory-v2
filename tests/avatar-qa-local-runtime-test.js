'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const {LocalOpenCvAvatarIdentityEvaluator}=require('../src/avatar-studio/local-opencv-avatar-evaluator');
(async()=>{const evaluator=new LocalOpenCvAvatarIdentityEvaluator();if(!fs.existsSync(evaluator.python)){console.log('Avatar QA local runtime integration skipped: run npm run avatar-qa:bootstrap.');return;}const truthSet={truthSetFingerprint:'test',references:[{contentHash:'solid'}],_references:[{contentHash:'solid',bytes:Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9WlRk9UAAAAASUVORK5CYII=','base64')}]};const result=await evaluator.evaluate({truthSet,candidateImage:truthSet._references[0].bytes});assert.equal(result.status,'UNCERTAIN');assert(result.reasonCodes.includes('NO_USABLE_FACE')||result.reasonCodes.includes('FACE_TOO_SMALL'));evaluator.close();console.log('Avatar QA local YuNet/SFace runtime test passed; local inference only, provider calls = 0.');})().catch((error)=>{console.error(error);process.exitCode=1;});
